# Phase 09 compatibility release

The first live HyperFlow Office report was correctly held because the broad memory envelope was stale. Reports now have a bounded, project-scoped current-source evidence projection instead of depending on unrelated derived summaries. Existing memory stores and derived freshness checks remain in Communications; nothing is extracted into another app.

Local validation: 262 unit tests and 35 database tests pass. Database evidence covers private/out-of-project exclusion, required scope, immediate eligibility revocation and omission of derived summaries. No provider operation or migration occurs. Release version 2.6.1; exact live fingerprint and consumer acceptance will be recorded after deployment.
