# Phase 09 compatibility release

The first live HyperFlow Office report was correctly held because the broad memory envelope was stale. Reports now have a bounded, project-scoped current-source evidence projection instead of depending on unrelated derived summaries. Existing memory stores and derived freshness checks remain in Communications; nothing is extracted into another app.

Local validation: 262 unit tests and 35 database tests pass. Database evidence covers private/out-of-project exclusion, required scope, immediate eligibility revocation and omission of derived summaries. No provider operation or migration occurs. Release version 2.6.1; exact live fingerprint and consumer acceptance will be recorded after deployment.

Release verified: PR 7 merged as 54fb77302782290e80bd00b6c747eb3061a56966. Hosted checkout installed that exact revision and passed 262 unit / 35 database tests. Replit published 2.6.1; production health returned status=ok and build=28ddcd984fa1, matching the local normalized source fingerprint. One health probe returned 500 during VM promotion; the subsequent published service matched the expected build. No migration was added.

Authenticated HyperFlow report preparation then returned current permitted source messages for the existing communications test project, including recording corrections and earlier controlled email/SMS/voice evidence. Report generation succeeded. A separate HyperFlow slide-layout repair followed; it does not change this source-evidence contract. Post-release Communications reruns passed 262 unit and 35 database tests. No new provider message or call was made.
