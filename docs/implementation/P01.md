# P01 — Communications transport authority implementation

Date: 8 September 2026. Status: implemented and locally verified; production activation pending.

Base: `1fabe2c`, preserving default `end_call` and its tests. Worktree: `C:/Users/joria/Documents/Codex/phase01-20260908/communications-service`.

## Changes

- Per-tenant backend email ceiling checked at the HTTP send handler and provider boundary; wildcard credentials cannot override configured draft-only restriction.
- Explicit `sms:send` and `voice:call` capabilities on canonical dispatch routes, in addition to communications:write. Draft-only scoped clients cannot inherit send authority from request bodies.
- Existing independently authorized, unlisted tenants retain their email behavior. Provider acceptance for an allowed tenant is tested with a mocked adapter.
- Svix pinned to 2.1.0 in manifest and lock to preserve the verified current return-value contract; package upgrade/migration is a separate decision.
- Shared authority fixture/schema, API supplement, backend examples and ownership/rollout docs.

## Verification

- Fresh `npm.cmd ci --ignore-scripts` completed. Three pre-existing dependency audit findings reported by installation were not auto-fixed.
- `npm.cmd run test:unit`: 232 passed, zero failed/skipped; [log](evidence/P01-unit.txt).
- Real Fastify injection confirms legacy wildcard policy denial and tenant spoof rejection before provider work. Scoped credential hashing/authentication and capability denials are exercised; all provider behavior is mocked.
- Existing signed email verifier tests pass under pinned version. Shared JSON fixture/schema and canonical event fixture content match HyperFlow.
- `git diff --check` passed. No database schema change; unpublished migration 019 remains in the original checkout and is not part of this branch. No full/live suite was run.

## Scope and ownership

No memory extraction or new service. Communications retains canonical identities, threads, provider transport and existing memory/enrichment; HyperFlow owns business policy and Asks. The normative ownership/authority contract is in [BOUNDARIES.md](../architecture/BOUNDARIES.md).

The implementation uses isolated `codex/phase01-boundaries` worktrees based on current remote main. Original dirty checkouts are unchanged. Unpublished ranked-threading/register changes are reserved for Phase 02, not silently included here.

## Activation gate

Production is **not activated** by this phase record. The actual CEO organization/tenant ID must be configured as draft_only in both backends. HyperFlow defaults unlisted tenants to draft-only; Communications deliberately preserves unlisted independent tenants' existing behavior. A user clarification for the production organization is pending.

Deploy transport support and its tenant ceiling before the HyperFlow consumer. Confirm named API clients have only intended SMS/voice capabilities. No migration, email, SMS, call or publication was performed in this goal. No signed callback or live-provider acceptance is claimed.

## Recovery

Keep the Communications restriction active during any HyperFlow rollback. Do not widen credentials or disable restrictions to hide policy errors. Existing operation receipts remain authoritative; reconcile any uncertain accepted operation before retry. No external effects were produced by these tests.

