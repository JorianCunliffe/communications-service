# Phase 08 — Ordered calendar observations

Status: released as v2.6.0. HyperFlow owns Google calendar credentials, calendar grants, proposals, booking and provider reconciliation. Communications continues to own normalized event context and participant enrichment. No booking endpoint or memory extraction is introduced.

Migration 023 serializes updates to one tenant/provider event and rejects stale or unversioned replacements of timestamped observations. Event and participant snapshots remain atomic. Exact normalized email identities enrich participants; cross-tenant contact/project/thread references are rejected. Cancelled observations no longer generate automatic meeting candidates. The public endpoint remains POST /v1/calendar/events.

The database fixture verifies correction, stale replay, participant removal, normalized contact matching and cancelled candidate exclusion. PR 6 merged as `f7c1e5bea18a59eb03fe9a9c80287194f19031a2`. Hosted development migration 023 completed and hosted tests passed 262 unit / 34 database. Replit publication now serves v2.6.0 with fingerprint `d8a49f0466b2`. The production Database overview independently showed 24 schema migration rows. Local post-release suites passed 262/262 unit and 34/34 database. Live Google observation/booking acceptance remains pending in HyperFlow.

An initial broad `npm test` invocation hit the HyperFlow development server on the shared default localhost port and failed its server-dependent checks, including HTML instead of health JSON. This was an incorrectly targeted harness run, retained in P08-release-wrong-target.txt. The correctly selected unit/database suites above are the release checks; that failed invocation is not a production regression or a passing live-provider suite.
