# P00 — Communications baseline and checkout decision

Date: 8 September 2026. This is the intended Communications implementation checkout for the programme. Existing application edits were preserved. Memory stays in this service.

## Git reconciliation

- Local HEAD `8c8b9d2` adds person-aware communication threading and migration 018.
- The older checkout at `C:/Users/joria/OneDrive/Documents/communications-service` is at `7ba163a`, an ancestor of this HEAD. Both remotes identify the same GitHub repository.
- This working copy adds ranked resolution, register/candidates/corrections, migration 019, threading documentation and PGlite tests. Its working package declares 2.4.0. These changes remain uncommitted and were not moved or reset.
- Freshly fetched GitHub main is `1fabe2c281ea1c08bbd3708de8f4c71cf93a73c8`: one commit after this HEAD, adding default `end_call` availability and tool tests. It changes `.env.example`, `config.js`, `package.json`, `test/context.test.js`, `test/tools.test.js`, and `tools.js`.
- Next integration must preserve both the local ranked-threading changes and the remote voice/tool changes, especially the overlapping package scripts. Do not replace the package manifest with either version wholesale. Include `test/tools.test.js` alongside the local `test:db` entry and dependency changes when resolving.

No merge was performed in baseline scope. The checkout selection is based on actual unpublished source, not its folder name or version number.

## Verification

| Check | Result | Evidence |
|---|---|---|
| `npm.cmd run test:unit` | 234 passed; no failures/skips | [Unit log](evidence/P00-unit.txt) |
| `npm.cmd run test:db` | 19 passed; no failures/skips | [Database log](evidence/P00-db.txt) |
| `git diff --check` | No whitespace errors; CRLF notices only | Observed command |
| Full test suite | Deliberately not run: server/provider/live Realtime cases | `test/suite.test.js` |

Database tests use isolated in-process PGlite and load migrations locally. They do not demonstrate production migration state. Unit checks use installed dependencies; no fresh-install test was performed. Manifest allows `svix ^2.1.0` while lock resolves 2.1.0; verifier compatibility on future dependency resolution remains a P01 reproducibility item.

## Contract baseline

The working `v1.js` contains register, candidate and rethread routes expected by HyperFlow. The ranked resolver uses deterministic lexical/rule scoring rather than an LLM call. Canonical threads and people remain here; HyperFlow owns workflow intent and accepted Ask outcomes.

Existing `memory.js`, `enrichment.js`, communication facts/extracted commitments, scoped context and loose-end routes remain in place. No extraction is planned. Legacy history and canonical-memory paths should be audited for coverage/scope in P03.

Gmail and Outlook adapters exist. Calendar ingestion is normalized context, not proof of live booking. The scheduler helper calls HyperFlow's tick endpoint and does not own business schedule policy.

## Available runtime evidence

`https://communications-service.replit.app/health` returned status `ok`, version `v2.3.0`, build `5d14bcff6ad6`, PostgreSQL persistence, enabled memory enrichment, durable events, voice context, outbound calls, and enforced Twilio signatures. Email reported enabled.

This health build identifier has not been reconciled to Git HEAD. It does not prove migration 018/019, register endpoint readiness, mailbox connection health, tenant-specific email authority or successful provider delivery. No authenticated business data/schema was inspected and no call/message was made.

The deployment-wide email flag is a reason to validate tenant/client-level CEO draft-only enforcement, not proof that an unauthorized message was sent.

## Fixtures, documentation and next work

Existing database fixture tenant is `thread_integration_test` with synthetic contacts and in-process requests. No live contact target or budget was provisioned. Use explicit controlled resources at the live acceptance gate.

Programme plan/status and HyperFlow evidence are in `C:/Users/joria/OneDrive/Documents/hyperflow-5/docs/`. P01 must preserve remote voice improvements while enforcing ownership/policy; P02 should validate and finish the existing ranked implementation, not rebuild it from the old checkout. Production migration and provider verification remain later acceptance gates.

No application source, deployed configuration or migration was changed. No push or release was performed. The baseline additions can be removed independently of all existing source edits.
